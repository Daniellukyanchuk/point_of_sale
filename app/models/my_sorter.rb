class MySorter

# Make a method sort_array 
# Method takes in an array of either strings or integers, 
# and returns an array with items sorted asc or desc. The default is ascending. 
# You are not allowed to use rails sort method, you have to write your own. 

  def self.sort_array(array)
    # a = []
    # a.push(min)
    array.each do |a|
      b += a.delete_at(a).get_min
    end

    

  end

  def self.get_min(min)
    

  end

  # def sort_me(array)
  # end
 






end